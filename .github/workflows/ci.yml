name: demo workflow
on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI Pipeline Running"
      
      - name: Build Project
        run: mvn clean package
